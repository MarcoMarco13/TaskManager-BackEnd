# Configura\u00E7\u00E3o do Banco de Dados
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/db_tasks}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:123456}

# Configura\u00E7\u00F5es do Hibernate / JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# Chaves com fallbacks de dev local
api.security.token.secret=${JWT_SECRET:minha-chave-secreta-local-12345}
recaptcha.secret-key=${RECAPTCHA_SECRET:6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe}

# Porta
server.address=0.0.0.0
server.port=${PORT:8080}